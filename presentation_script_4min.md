# 基于大模型的人才智能匹配系统 —— 4分钟讲稿

大家好，我今天介绍基于大模型的人才智能匹配系统，按使用流程来讲。

用户从登录页选择个人求职者或企业招聘方身份，登录后进入工作台。工作台展示统计数据和快捷导航卡片，帮助用户快速定位功能。个人用户导航是能力图谱、职位推荐、我的意向；企业对应职位档案、候选人才、意向管理。两类用户操作流程完全对称，背后共享同一套解析和匹配引擎。

个人用户第一步是能力图谱。上传 PDF 或 Word 简历后点击解析，系统先提取文本，再调用大模型——这是第一个核心场景。简历文本被嵌入精心设计的 Prompt 发给大模型，要求严格输出 JSON 格式，包含姓名、学历、技能列表、工作年限、期望城市五个字段。Prompt 里明确了命名规则和取值约束，比如工作经验只计正式工作、技能独立列出。大模型返回后做输出净化：去除 markdown 代码标记，用花括号平衡算法精确截取合法 JSON 片段，确保下游拿到干净数据。生产层面还设计了兜底——大模型超时或返回空，自动回退到基于正则的基础提取，功能不会中断。解析完成后，页面用 ECharts 雷达图展示五个能力维度的分布，用力导向图可视化技能关系网络和连接强弱，所有数据来自结构化解析结果。

第二步是职位推荐，核心环节。系统根据简历与所有职位做多维度匹配，按匹配度取前十个。匹配引擎分四维评分：学历 20 分、城市 20 分、Jaccard 系数技能相似度 30 分、余弦相似度 30 分，总分 100。用两种算法因为 Jaccard 看集合交叠比例，余弦看技能频次权重，两者互补避免单一指标偏差。推荐卡片上用进度条和颜色区分匹配度——80 以上绿色、60 到 80 橙色、以下红色，同时展示已掌握与缺失技能的对比分析，用户可直接点击发送意向向企业发起沟通。

企业视角对称。先在职位档案上传职位描述，大模型解析出岗位名称、学历要求、技能、经验、城市。再到候选人才页匹配所有求职者，基于同样的四维评分模型展示匹配度排序。关键设计在于：简历和职位数据在同一个语义空间内，大模型结构化输出使字段维度统一，匹配引擎在标准化数据层运算，保证了双向推荐一致。匹配完成后进入意向管理，接收方点击接受，双方联系方式自动解锁，完成从匹配到沟通的闭环。

最后说架构。大模型调用抽象成 LLMService 接口，三种实现：DeepSeek API 云端主推、Ollama 本地部署适合隐私场景、通用云厂商可选。改一行配置就能切换，不影响上层业务逻辑。整个系统分四层：采集解析、大模型语义理解、匹配计算、可视化展示，层间通过标准 DTO 传递数据。

总结。用户从登录、上传、大模型解析、可视化展示、匹配推荐到意向沟通，走完完整闭环。大模型扮演两个角色——结构化解析器把非结构化文本转化为可计算的维度数据，语义底座让人才和岗位在同一体系中可比对。大模型是能力组件不是银弹，需要合理的架构、异常降级和灵活部署来支撑生产运行。

谢谢大家。
