【项目背景 - 接续 M3 文件上传】
项目已完成 M1（基建）、M2（登录注册）、M3（文件上传）。
当前状态：
- 后端：SpringBoot 8080，已配置 CORS + Session
- 前端：Vue3 + Vite 5173，登录页已重构为深蓝政务风格（主色 #1E3A8A）
- 数据库：`resume` 和 `job` 表已有 `file_path` 数据（来自 M3 上传），`parsed_json` 字段为空
- 实体类：`Resume.java`、`Job.java` 已存在，`parsed_json` 字段类型为 String
- Maven依赖：已包含 Spring Web、MyBatis-Plus、MySQL驱动、Lombok

【本次任务：开发 M4 文档智能解析模块】
请严格按照以下要求生成代码，严禁越界。

--- 第一步：添加 Maven 依赖（在 pom.xml 中追加） ---
请在 `<dependencies>` 标签内新增以下依赖：
1. Apache PDFBox（解析PDF）：`pdfbox` 版本 2.0.29
2. Apache POI（解析Word）：`poi` 版本 5.2.5 和 `poi-ooxml` 版本 5.2.5

--- 第二步：后端开发（Java） ---
1. 生成 `ParseService.java`（放在 `com.match.service` 包）及其实现类 `ParseServiceImpl.java`：
   - 提供方法 `String extractTextFromFile(String filePath)`：根据文件后缀（.pdf/.doc/.docx），调用 PDFBox 或 POI 提取纯文本内容。
   - 提供方法 `Map<String, Object> parseResume(String text)`：调用大模型API，从简历文本中提取姓名、学历、技能列表、工作年限、期望城市，返回 Map（key为字段名）。
   - 提供方法 `Map<String, Object> parseJob(String text)`：调用大模型API，从职位描述中提取职位名称、学历要求、技能要求、工作年限要求、工作城市，返回 Map。
   - 大模型调用封装：创建 `LLMService.java` 接口，实现类 `OllamaLLMServiceImpl`（本地）和 `CloudLLMServiceImpl`（云端），通过 `application.yml` 中的 `llm.provider` 配置切换（值为 local 或 cloud）。
   - 大模型提示词模板：
     - 简历：`"从以下简历文本中提取：姓名、学历（字符串）、技能列表（数组）、工作年限（数字）、期望城市（字符串）。返回合法JSON格式，不要有其他文字。\n\n文本：{text}"`
     - 职位：`"从以下职位描述中提取：职位名称、学历要求（字符串）、技能要求（数组）、工作年限要求（数字）、工作城市（字符串）。返回合法JSON格式，不要有其他文字。\n\n文本：{text}"`

2. 生成 `ParseController.java`（放在 `com.match.controller` 包）：
   - 提供 `POST /api/parse/{type}/{id}` 接口（type 为 "resume" 或 "job"，id 为对应的记录ID）。
   - 从 `HttpSession` 获取当前 `userId`，校验是否登录。
   - 根据 type 从 `resume` 或 `job` 表中查询记录，获取 `file_path`。
   - 调用 `ParseService.extractTextFromFile()` 提取文本。
   - 调用 `ParseService.parseResume()` 或 `parseJob()` 获取结构化 Map。
   - 将 Map 转为 JSON 字符串，更新到对应表的 `parsed_json` 字段。
   - 返回 `Result.success("解析成功")`。

3. 生成 `ResumeService.java` 和 `JobService.java`（放在 `com.match.service` 包）：
   - 提供 `Resume getById(Long id)` 和 `Job getById(Long id)` 方法。
   - 提供 `void updateParsedJson(Long id, String json)` 方法。

4. 生成 `ResumeServiceImpl.java` 和 `JobServiceImpl.java` 实现类（使用 MyBatis-Plus 的 `updateById` 方法）。

5. 在 `application.yml` 中增加大模型配置项：
   ```yaml
   llm:
     provider: local  # local 或 cloud
     local:
       url: http://localhost:11434/api/generate
       model: qwen2:7b
     cloud:
       api-key: your-api-key
       url: https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation
--- 第三步：前端开发（Vue3 + 原生HTML/CSS，保持深蓝政务风格） ---

修改 frontend/src/views/Personal.vue（个人工作台）：

在上传区域下方，增加“解析简历”按钮。

点击后，调用 POST /api/parse/resume/{resumeId}（resumeId 从上传成功后返回的数据中获取，或从后端查询当前用户最新简历）。

解析过程中显示“解析中...”状态（按钮禁用）。

解析成功后，在页面上显示解析出的结构化数据卡片：

姓名、学历、技能标签（圆角灰色方块）、工作年限、期望城市。

这些展示元素使用深蓝主色（#1E3A8A）作为标题色，卡片背景白色，边框浅灰（#e8ecf1）。

若解析失败，显示错误提示。

修改 frontend/src/views/Enterprise.vue（企业工作台）：

在上传区域下方，增加“解析职位”按钮。

点击后，调用 POST /api/parse/job/{jobId}。

解析成功后，展示：职位名称、学历要求、技能要求（标签）、工作年限要求、工作城市。

样式与 Personal.vue 保持一致。

前端新增 api/parse.js 或直接在组件中封装 axios 调用：

parseResume(id) -> POST /api/parse/resume/${id}

parseJob(id) -> POST /api/parse/job/${id}

解析成功后，展示的数据使用与登录页一致的字体颜色（深灰 #333）和卡片阴影（box-shadow: 0 2px 8px rgba(0,0,0,0.08)）。

--- 🚨 硬性限制（绝对禁止违反） ---

严禁修改登录、注册、上传功能的任何逻辑和样式。

严禁修改 user 表结构。

严禁修改 application.yml 中的端口、数据库连接（只允许新增 llm 配置段）。

严禁改动任何路由配置（router/index.js 只允许新增路由，不允许修改已有路由）。

大模型调用失败时，必须返回明确的错误信息（如“大模型服务不可用”），不允许系统崩溃。

前端严禁使用 Element Plus、Ant Design 等 UI 库，保持原生 HTML + CSS，与现有登录页风格一致（主色 #1E3A8A，白色卡片，柔和阴影）。

后端严禁硬编码大模型 API Key（必须从 application.yml 读取）。

请分别给出：

pom.xml 中新增的依赖片段

后端的 ParseController.java、ParseService.java、ParseServiceImpl.java、LLMService.java、OllamaLLMServiceImpl.java、CloudLLMServiceImpl.java、ResumeService.java、ResumeServiceImpl.java、JobService.java、JobServiceImpl.java 完整代码

前端的 Personal.vue 和 Enterprise.vue 完整代码

更新后的 application.yml 完整内容

text

---

## ⚠️ 重要提示（执行前阅读）

1. **大模型调用策略**：
   - 如果本地没有 Ollama 或不想配置，可让 AI 在 `OllamaLLMServiceImpl` 中**返回模拟数据**（硬编码一个 JSON 示例），便于先跑通流程。等演示前再替换为真实 API 调用。
   - 模拟数据示例：
     ```json
     { "name": "张三", "education": "本科", "skills": ["Java", "Spring", "MySQL"], "experience": 5, "city": "成都" }
前端数据展示：解析结果展示区紧贴上传区下方，使用卡片样式，保持整体视觉统一。

Git 提交节点：M4 跑通后立即执行 git add . && git commit -m "M4 文档智能解析完成"。





【紧急修复任务：M4 文档智能解析数据提取不准确】
项目背景：M1、M2、M3 已跑通，M4 解析功能已实现（能调 API、能写库），但解析出的 JSON 数据（如姓名、学历、技能）为空或格式错误。

当前问题现象：
- 调用 `/api/parse/resume/{id}` 接口成功，数据库 `parsed_json` 字段有值，但不是预期的结构化数据（可能是大模型返回了多余文字，或 JSON 键名不匹配）。
- 前端展示时字段空白或显示 `undefined`。

【本次任务：修复 ParseService 解析逻辑，增强鲁棒性】
请仅修改 `ParseServiceImpl.java` 和相关的 `LLMService` 实现类，严禁改动 Controller、Mapper、前端 UI 和配置文件（除非必须）。

--- 修复点 1：强化大模型提示词（强制输出纯 JSON） ---
修改 `ParseServiceImpl.java` 中的提示词模板，增加更强硬的指令：
- 简历提示词改为：
"你是一个严格的信息提取工具。从以下简历文本中提取信息，只返回一个合法的JSON对象，不要包含任何其他文字、解释或markdown标记（如```json）。\n" +
"JSON格式必须为：{"name":"姓名", "education":"学历", "skills":["技能1","技能2"], "experience":数字(工作年限), "city":"期望城市"}。\n\n" +
"文本内容：\n" + text

text
- 职位提示词类似，强制返回 `{"title":"职位名", "eduReq":"学历要求", "skillReq":["技能1"], "expReq":数字, "city":"城市"}`。

--- 修复点 2：增加 JSON 响应清洗函数（防止 Markdown 污染） ---
在 `ParseServiceImpl.java` 中新增一个私有方法：
```java
private String cleanJsonResponse(String raw) {
  // 1. 去除 Markdown 代码块标记（```json 和 ```）
  String cleaned = raw.replaceAll("(?i)```json\\s*", "").replaceAll("```\\s*", "");
  // 2. 去除首尾空白字符
  cleaned = cleaned.trim();
  // 3. 如果字符串以 "{" 开头且以 "}" 结尾，直接返回；否则尝试正则提取 {} 内的内容
  if (cleaned.startsWith("{") && cleaned.endsWith("}")) {
      return cleaned;
  } else {
      // 取第一个 { 和最后一个 } 之间的内容
      int start = cleaned.indexOf("{");
      int end = cleaned.lastIndexOf("}");
      if (start != -1 && end != -1 && start < end) {
          return cleaned.substring(start, end + 1);
      }
      return cleaned; // 实在不行原样返回，让上层抛异常
  }
}
--- 修复点 3：增强 JSON 解析容错（字段缺失时给默认值） ---
在调用大模型返回后，使用 Jackson 解析，并使用 @JsonProperty 或 optXXX 方式，避免因某个字段缺失导致整个解析失败。

如果 experience 解析失败，默认设为 0。

如果 skills 不是数组，默认设为空数组 []。

--- 修复点 4：增加调试日志（方便你排查） ---
在 ParseServiceImpl 中，将大模型返回的 原始字符串 和 清洗后的字符串 打印到控制台（使用 log.info 或 System.out.println）。
这样你下次解析时，可以在 IDEA 控制台直接看到大模型到底“说”了什么。

--- 前端微调（仅增强显示鲁棒性） ---
修改 Personal.vue 和 Enterprise.vue 中展示数据的部分，使用 v-if 判断数据是否存在，如果为空显示“暂未提取到该信息”，防止页面报错。

--- 🚨 硬性限制 ---

严禁修改登录、注册、上传页面的样式和逻辑。

严禁修改数据库表结构。

严禁修改 application.yml 中已存在的配置（如端口、数据库连接）。

只修改 ParseServiceImpl.java、LLMService 实现类、以及前端对应的解析结果展示组件。

不要更改主色或 UI 框架，保持深蓝（#1E3A8A）政务风格。

请分别给出：

修复后的 ParseServiceImpl.java 完整代码（含 cleanJsonResponse 方法和增强提示词）。

修复后的 Personal.vue（仅解析结果展示部分，不要动上传按钮区域）。

text

---

### 🔍 拿到代码后，如何验证修复效果？

1. **先看控制台日志**：重启后端，上传一份**内容清晰的简历文本**（比如含“张三”、“本科”、“Java、Spring”等明确关键词的 `.docx` 文件）。
2. **点击“解析简历”**，观察 IDEA 控制台输出的 **“大模型原始返回”** 和 **“清洗后的JSON”**。
3. 如果 `清洗后的JSON` 能正常显示为 `{"name":"张三","education":"本科"...}`，说明修复成功；如果仍然是乱码或空白，把控制台打印的**原始返回内容**复制给我，我帮你人工分析大模型响应格式。

---

### ⚠️ 如果大模型服务无法正常调用（备选方案）

如果你本地 Ollama 没装好，或者不想消耗 API 额度，可以临时让 AI 在 `OllamaLLMServiceImpl` 中**返回 Mock 数据**（模拟解析结果），这样至少能保证前端展示和后续 M5/M6 开发不受阻塞。

> 告诉 Trae：*“如果大模型调用失败，请在 `OllamaLLMServiceImpl` 中返回以下模拟 JSON 以便继续开发：`{"name":"测试用户", "education":"本科", "skills":["Java","Vue"], "experience":3, "city":"成都"}`”*

---