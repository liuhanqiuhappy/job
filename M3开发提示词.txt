【项目背景 - 接续 M2 登录注册】
我的招聘匹配平台项目 M1（基建）和 M2（登录注册）均已跑通。
- 后端：SpringBoot 运行在 8080 端口，已配置好 CORS 和静态资源路径。
- 前端：Vue3 + Vite 运行在 5173 端口，已配置 `/api` 代理到后端。
- 现有实体类：`User`（已用）、`Resume`（空壳）、`Job`（空壳）。数据库 `resume` 和 `job` 表已存在，包含 `id`、`user_id`、`file_path`、`parsed_json`、`create_time` 字段。
- 登录状态：用户登录后，`userId` 和 `role` 已存入 `HttpSession`。

【本次任务：开发 M3 文档上传模块（仅本地存储，不解析内容）】
请严格按照以下要求生成代码，严禁越界。

--- 后端开发要求（Java） ---
1. 生成 `UploadController.java`（放在 `com.match.controller` 包）：
   - 提供 `POST /api/upload` 接口。
   - 接收参数：`MultipartFile file` 和 `String type`（固定值为 "resume" 或 "job"）。
   - 从 `HttpSession` 中获取 `userId`（key 为 "userId"）。若获取不到，返回 `Result.error("请先登录")`。
   - 文件校验：只允许 `.doc`、`.docx`、`.pdf` 后缀，文件大小限制为 10MB。
   - 存储逻辑：
     - 保存路径：项目根目录下的 `uploads/` 文件夹，按日期分子目录（如 `uploads/2026-07-05/`）。
     - 文件名：用 `UUID` + 原始后缀（如 `a1b2c3d4.docx`）。
   - 数据库操作（使用 MyBatis-Plus）：
     - 如果 `type` 为 "resume"，向 `resume` 表插入记录（`user_id`=当前用户id，`file_path`=保存的完整路径）。
     - 如果 `type` 为 "job"，向 `job` 表插入记录。
   - 返回 `Result.success("上传成功")`。
2. 生成 `ResumeMapper.java` 和 `JobMapper.java`（放在 `com.match.mapper` 包）：
   - 分别继承 `BaseMapper<Resume>` 和 `BaseMapper<Job>`，无需写任何额外代码。
3. 在 `application.yml` 中增加上传文件大小配置：
   - `spring.servlet.multipart.max-file-size: 10MB`
   - `spring.servlet.multipart.max-request-size: 10MB`

--- 前端开发要求（Vue3 + 原生HTML） ---
1. 修改 `frontend/src/views/Personal.vue`（个人工作台页面）：
   - 在现有的“欢迎”文字下方，增加一个**文件上传区域**。
   - 需要一个点击选择文件的 `<input type="file">`，也支持拖拽上传（用 `@drop` 和 `@dragover` 事件实现，或者简单起见先只做点击上传）。
   - 上传时调用 `/api/upload`，`type` 固定传 `"resume"`。
   - 使用 `FormData` 对象发送请求，上传过程中显示“上传中...”状态，上传成功后显示“简历 xxx 上传成功！”。
2. 修改 `frontend/src/views/Enterprise.vue`（企业工作台页面）：
   - 同样增加上传区域，但 `type` 固定传 `"job"`。
3. 确保前端在发送请求时携带 Cookie（`axios.defaults.withCredentials = true`），以便后端能获取到 Session。

--- 🚨 硬性限制（绝对禁止违反） ---
1. **严禁解析文件内容**（不要引入 PDFBox 或 POI，不要读取文件里的文字）。
2. **严禁计算匹配分、生成图谱或修改 `parsed_json` 字段**。
3. **严禁修改 `user` 表结构**。
4. **严禁改动 M2 的登录逻辑和路由配置**。
5. 前端不要使用任何 UI 组件库，保持原生 HTML + CSS。

请分别给出后端 Java 代码（Controller + Mapper）和前端 Vue 代码（Personal.vue + Enterprise.vue），以及前端需要添加的 axios 配置修改。